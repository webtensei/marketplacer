{
  "name": "prisma-client",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/prisma-client/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project prisma-client --web",
  "targets": {
    "prisma": {
      "command": "prisma",
      "options": {
        "cwd": "libs/prisma-client"
      }
    },
    "generate-types": {
      "command": "prisma generate",
      "options": {
        "cwd": "libs/prisma-client"
      }
    },
    "migrate": {
      "command": "prisma migrate dev --name migration-unnamed",
      "options": {
        "cwd": "libs/prisma-client"
      }
    }
  }
}
